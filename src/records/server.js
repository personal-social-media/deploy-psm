export default class Server {
  constructor(ip, username, password, privateKey) {
    this.ip = ip;
    this.username = username;
    this.password = password;
    this.privateKey = privateKey;
  }
}
